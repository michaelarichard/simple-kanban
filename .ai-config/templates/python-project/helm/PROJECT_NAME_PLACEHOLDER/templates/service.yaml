apiVersion: v1
kind: Service
metadata:
  name: {{ include "PROJECT_NAME_PLACEHOLDER.fullname" . }}
  labels:
    {{- include "PROJECT_NAME_PLACEHOLDER.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "PROJECT_NAME_PLACEHOLDER.selectorLabels" . | nindent 4 }}
