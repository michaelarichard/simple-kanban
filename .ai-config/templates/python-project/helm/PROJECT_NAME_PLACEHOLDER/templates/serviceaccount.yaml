{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "PROJECT_NAME_PLACEHOLDER.serviceAccountName" . }}
  labels:
    {{- include "PROJECT_NAME_PLACEHOLDER.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
