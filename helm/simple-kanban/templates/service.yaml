apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-kanban.fullname" . }}
  labels:
    {{- include "simple-kanban.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple-kanban.selectorLabels" . | nindent 4 }}
